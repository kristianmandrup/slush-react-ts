import { expect } from 'chai';

import * as <%= modelClassName %>Actions from '../<%= modelName %>';

describe('<%= modelClassName %>Actions', () => {
  it('creates new <%= modelName %>', () => {
    // expect(<%= modelName %>).to.be.a(<%= modelClassName %>);
  });
});
